/**
 * Bab 2 — Capaian Pelaksanaan Aktualisasi (hub)
 * Setiap sub-bab ditulis di file terpisah dengan format 2-<n>-<nama>.typ.
 * Untuk menambah sub-bab baru, buat file baru lalu tambahkan #include di bawah.
 */

= CAPAIAN PELAKSANAAN AKTUALISASI

#show figure.caption: set text(weight: "bold")

// 2.1: intro portrait, lalu switch ke landscape untuk tabel dan seluruh sub-bab berikutnya
#include "2-1a-realisasi-jadwal-intro.typ"
#set page(flipped: true)
#include "2-1b-realisasi-jadwal-tabel.typ"
#include "2-2-hasil-pelaksanaan.typ"
#include "2-3-realisasi-internalisasi.typ"
#include "2-4-dampak-aktualisasi.typ"
#include "2-5-rencana-tindak-lanjut.typ"
